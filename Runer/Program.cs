using DAL.Model;
using Northwind.DAL.Repositories;

class Program
{
    static async Task Main(string[] args)
    {

        //BaseRepository<Person> repos = new BaseRepository<Person>(new ConfigurationProvider(), "person");
        //var entities = await repos.GetAllAsync();

        //foreach (var item in entities)
        //{
        //    await Console.Out.WriteLineAsync($"{item.name} - {item.surname}");
        //}

    }
}